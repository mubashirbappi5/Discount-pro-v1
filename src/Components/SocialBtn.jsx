import React from 'react';

const SocialBtn = () => {
    return (
        <div>
            <button className='btn rounded-lg'>Continue with Google</button>
            
        </div>
    );
};

export default SocialBtn;